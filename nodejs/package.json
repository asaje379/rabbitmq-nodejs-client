{
  "name": "@ensfierte/rmq-connector",
  "version": "1.0.28",
  "description": "",
  "main": "dist/main.js",
  "types": "dist/main.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only ./src/main.ts",
    "build": "tsc && esbuild src/main.ts --bundle --minify --platform=node --packages=external --outdir=dist",
    "start": "node dist/main.js"
  },
  "keywords": [
    "rabbit mq",
    "message broker"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/amqplib": "^0.10.5",
    "@types/node": "^22.5.0",
    "esbuild": "^0.23.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "amqplib": "^0.10.4",
    "nanoid": "^3.3.7"
  }
}
